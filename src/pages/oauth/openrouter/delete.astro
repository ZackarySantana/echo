---
import { localsUser } from "../../../lib/auth";
import { setOpenRouterAPIKey } from "../../../lib/db";

const [user, resp] = localsUser(Astro.locals);
if (resp) {
    return resp;
}

await setOpenRouterAPIKey(user.id, null);

return Astro.redirect("/settings");
---
