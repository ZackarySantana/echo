---
import { type HTMLAttributes } from "astro/types";
import { cn } from "../../lib/cn";

interface Props extends HTMLAttributes<"h1" | "h2"> {
    type: "heading" | "subheading" | "tertiary" | "body" | "caption";
}

const { type, class: className, ...rest } = Astro.props;
---

{
    type === "heading" && (
        <h1 class={cn("text-3xl font-bold", className)} {...rest}>
            <slot />
        </h1>
    )
}

{
    type === "subheading" && (
        <h2 class={cn("text-xl font-bold", className)} {...rest}>
            <slot />
        </h2>
    )
}

{
    type === "tertiary" && (
        <p class={cn("text-md font-semibold", className)} {...rest}>
            <slot />
        </p>
    )
}

{
    type === "body" && (
        <p class={cn("text-sm", className)} {...rest}>
            <slot />
        </p>
    )
}
